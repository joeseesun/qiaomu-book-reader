# Issues #17–#19 评估与修复

独立分支 `codex/issues-17-18-19`，已同步主分支 `9e71b00`（PR #23 的 reading-workflow 改动）。保留目录/搜索导航等已合并功能，不更改版本号或发布 Release。

| Issue | 结论 | 本次处理 |
| --- | --- | --- |
| #17 自定义字体 | 有效功能缺口 | 在全局外观和书内阅读设置加入「自定义字体」：可搜索并预览本机字体，也可浏览导入 TTF、OTF、WOFF、WOFF2。已导入字体可直接复用，文件保存于仓库。手动填写 font-family 收进折叠选项。选中的字体随设备外观保存。 |
| #18 翻页按钮常驻 | 有效功能缺口 | 增加「鼠标靠近时显示 / 常驻显示」，默认沿用原有工具栏行为。常驻模式将原有两个按钮移到左右两侧，保留事件和键盘操作；两侧预留空间，避免盖住正文。切换后重排并沿用阅读锚点机制；滚动模式隐藏箭头。兼容 PR #23 的目录/搜索栏，切回默认模式时保留工具栏控件顺序。 |
| #19 PDF 脚注破坏排版 | 3.9.1 的文字重排路径可造成截图中的断行；4.0.0 起已有原页渲染 | 核对旧版 `pdfItemsToHtml` 路径和现有 `extractPdf → pdfPageShell` 路径，新增含上标与页底脚注的原创 PDF 回归样本及测试，防止回退到文字重排。本次没有再改 PDF 渲染算法。 |

## 验证

- 独立工作树 `npm ci`。
- `npm test`：145/145，覆盖字体枚举、格式校验、解码失败、导入去重、文件读回、重载、同步文件修复、路径限制和既有阅读回归。
- `npm run check:i18n`：1,208 个英文键有中文覆盖。
- `npx eslint src/ --max-warnings 0`、`npm run build`、`npm run build:community`、`npm run verify:release` 通过；标准构建重复构建哈希一致。
- 浏览器隔离环境运行与生产构建同源的 `ReadSettingsModal`、`Paginator`、`extractPdf` 和按钮控制器；Obsidian 的容器及少量宿主 API 使用测试替身。
- 实际键盘输入 `monospace` 后，预览和正文计算字体均为 `monospace`；常驻按钮在工具栏外，点击下一页有效。默认模式恢复为原来的底部工具栏按钮，没有重复创建按钮。
- 867px 和 390px 阅读容器检查：按钮与阅读区域不重叠，页面没有横向溢出。390px 容器不是手机实机验收。
- 原创 PDF 在浏览器中实际渲染为 1224×1584 页图。上标前后正文的文字层 top 均为 12.99%，引用上标为 12.84%，页底脚注为 89.13%；原页图像保留对应位置。PDF 不显示重排字体设置。

- 原生字体列表返回 851 个字体样式，按家族去重为 293 种；键盘搜索并选择 PingFang SC 后正文实际应用。
- 通过原生文件窗口导入 Arial.ttf，显示已保存；磁盘文件 773,236 字节，SHA-256 与记录一致。销毁测试实例并从保存数据重新创建后，字体重新加载为 `loaded`，正文继续使用导入字体；390px 阅读容器显示正常。此项不是应用重启或手机实机验收。

## 范围与待验证项

- 本机字体仅在用户点击时枚举；不支持或拒绝访问时，界面提示使用文件导入。本机字体名称只在安装了该字体的设备上生效。
- 导入文件最大 64 MB，经字体解码器验证后保存到 `_attachments/qiaomu-reader-fonts/`，按内容哈希去重并校验读回。字体目录需由用户的仓库同步工具同步；文件未到达或损坏时提示并回退字体，重新导入可修复。字体文件没有嵌入插件发布包。
- #19 未提供原始 PDF，无法对报告者的那本书逐页比对。截图已检查，旧版路径已核实；新增样本验证不能替代原书复验，因此不自动关闭 #19。
- 新字体流程在 Obsidian 1.13.7 的独立测试弹窗中使用真实 Modal、FuzzySuggestModal、系统字体 API、原生文件选择器及 FontFace 验证；插件设置与文件适配器指向临时目录。原有插件安装未覆盖；`verify:release` 的 `installVerified` 为 false。
- 合并和正式发布前仍需在独立 Obsidian vault 验收桌面/移动阅读器、跨重启设置恢复和阅读锚点；不应以 CI 通过替代这些检查。
