# Qiaomu Book Reader 3.8 本地产品与健壮性审计

状态：本地候选版，尚未发布。用户确认 Rockfish 本地体验后才允许创建 PR 或 Release。

## 核心流程

书库 → 打开图书 → 恢复位置 → 阅读/翻页 → 选文 → 划线、复制、批注、AI → 自动同步阅读笔记 → `↩` 返回原文。

插件设置按任务分为阅读、外观、笔记、AI 与翻译、存储与同步、关于；书内“阅读设置”只负责高频阅读外观与排版。

## P0

| 问题 | 风险 | 状态与证据 |
| --- | --- | --- |
| 连续翻页会并发写进度，旧写入可能晚于新写入完成 | 恢复到旧位置 | 已修复：进度写入串行；队列有顺序与失败恢复测试 |
| `data.json` 多处并发保存 | 设置、备份快照互相覆盖 | 已修复：本地数据保存使用同一串行队列 |
| 损坏或同步未完成的 JSON 被当成空对象 | 下一次保存覆盖唯一可恢复文件 | 已修复：拒绝数组/无效 JSON，保存原始副本并锁定该数据文件，重新读到合法文件后才解锁 |
| 划线/批注保存失败仍提示成功 | 用户误以为内容已落盘 | 已修复：新增、删除、改色和批注统一检查结果并限频提醒 |
| 打开图书失败只显示短暂通知 | 页面停在空白加载状态 | 已修复：桌面与移动端共用可重试错误页，含返回书库入口与折叠技术详情 |
| 书库封面被键盘按钮的主题背景遮挡 | 书库核心内容不可见 | 已修复并在 Rockfish 真实书库验证四本封面恢复 |

## 高价值 P1

| 问题 | 状态 |
| --- | --- |
| 桌面与移动端各自复制 EPUB/FB2/PDF 加载分支 | 已抽取统一 `loadReaderDocument`，降低两端行为漂移 |
| 选文工具条切换到其他标签后仍停留 | 已在活动标签变化时关闭 |
| Obsidian 1.13 无法索引插件设置 | 已接入 `getSettingDefinitions`；可按插件名定位设置页 |
| 书库封面、书名、工具按钮缺少键盘入口 | 卡片支持键盘打开，封面显示和图书操作使用原生按钮 |
| 阅读器主要图标按钮缺少语义 | 顶部、底部导航改为原生按钮并补充中文标签 |
| 发布资产与 Rockfish 安装文件靠人工核对 | 新增 `npm run verify:release -- <插件目录>`，统一检查 ID、版本与三个发布资产哈希 |
| 运行日志仍混用上游产品名 | 运行期错误前缀统一为 Qiaomu Book Reader；许可与鸣谢保持不变 |
| DeepSeek 思考模式无法按阅读任务取舍速度 | 新增独立开关；默认保持官方思考模式，关闭后请求明确使用非思考模式 |
| AI 高级设置在仅使用推荐模型时仍自动展开 | 只在模型或接口确实被自定义时展开，默认配置保持收起 |

## P2 / 延后

| 项目 | 原因 |
| --- | --- |
| 将约 1.1 万行 `src/main.js` 全面拆分模块 | 风险面过大；本轮只抽取持久化与加载的高风险重复逻辑，待本地候选版稳定后分批进行 |
| 在真实 Obsidian 打开 PDF 与 FB2 | Rockfish 当前只有四本 EPUB；不向用户仓库导入测试书。构建仍包含两种解析器，后续使用用户提供或明确授权的样本验证 |
| 真机手机与电子墨水屏验收 | 当前只有桌面 Obsidian；已有窄屏/触控 CSS 与静态回归，仍需真机证据 |
| 发布到 GitHub、合并 PR、创建 BRAT Release | 用户明确要求本地测试通过后再发布 |

## 当前验证

- `npm test`：24 项通过；包含损坏 JSON 原文备份、读取失败不伪造备份路径、串行队列失败恢复等真实存储回归。
- `npm run check:i18n`：中文覆盖检查通过。
- `npx eslint src/`：0 error；仅保留专有产品名和示例路径的 sentence-case warning。
- `npm run build`：通过，PDF worker 已嵌入。
- Rockfish：书库四本封面正常；点击卡片可打开 EPUB；阅读笔记的 `↩` 可精确返回原文并闪烁定位；阅读设置无横向滚动，竖向内容在弹层内滚动。
- Rockfish 选文流程：已有划线可打开紧凑工具条，依次显示三种颜色、AI、复制、批注和“更多”；AI 对话框展示所选原文、六个快捷问题、输入框和提示词管理入口；提示词管理弹层可在内部滚动且不横向溢出。本轮只读验收未发送 AI 请求、未修改划线或用户提示词。
- Rockfish AI 设置：DeepSeek 密钥只显示为钥匙串占位符；新增“思考模式”开关；使用推荐模型和官方接口时“高级设置”默认收起。未发送连接测试或书籍内容。
- 安装边界：只覆盖 `main.js`、`manifest.json`、`styles.css`，未覆盖 `data.json`、`thumb-cache.json`、钥匙串或用户内容。
